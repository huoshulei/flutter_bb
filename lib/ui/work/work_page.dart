import '../../config.dart';

class WorkPage extends StatefulWidget {
  @override
  State<StatefulWidget> createState() {
    return _WorkState();
  }
}

class _WorkState extends BaseState<WorkPage> {
  @override
  void initState() {
    super.initState();
  }

  @override
  void didUpdateWidget(WorkPage oldWidget) {
    super.didUpdateWidget(oldWidget);
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: ListView(
      children: <Widget>[
      ],
    ));
  }
}
